const listafrutas: string[] = ['Banana', 'Maça', 'Abacaxi', 'Pera', 'Uva', 'Manga']

let i = 0 
// While = Enquanto a condição for ---- ira ser execultado 
while (i < listafrutas.length) {
    console.log(listafrutas[i])
    i++
}